{
  "name": "express-mongodb-project1",
  "version": "1.0.0",
  "description": "Food calorie management system backend built with Node.js, Express, and MongoDB.",
  "main": "dist/server.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/server.js",
    "dev": "nodemon --watch . --ext ts,json --ignore dist --ignore public --ignore notebooklm-pack --exec \"npm run build && node dist/server.js\"",
    "test": "npm run build && node dist/tests/auth-food-admin.test.js"
  },
  "keywords": [
    "express",
    "mongodb",
    "mongoose",
    "jwt",
    "food-calorie"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "dotenv": "^16.6.1",
    "express": "^5.1.0",
    "jsonwebtoken": "^9.0.3",
    "mongodb": "^7.1.1",
    "mongoose": "^8.19.1"
  },
  "devDependencies": {
    "@types/express": "^5.0.5",
    "@types/jsonwebtoken": "^9.0.10",
    "@types/node": "^24.10.1",
    "nodemon": "^3.1.10",
    "typescript": "^5.9.3"
  }
}
